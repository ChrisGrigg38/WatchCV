{
  "name": "watchcv",
  "version": "1.0.0",
  "scripts": {
    "setup-db": "node scripts/setup-mongodb.js",
    "dev": "netlify env:import .env && netlify dev --target-port 5173",
    "build": "cd webapp && npm run build",
    "install:all": "npm install && cd webapp && npm install"
  },
  "dependencies": {
    "mongodb": "^6.3.0",
    "uuid": "^9.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@netlify/functions": "^2.4.0"
  }
}